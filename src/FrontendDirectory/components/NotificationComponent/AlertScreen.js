import { Text, View } from "react-native";

const AlertScreen = ({notifications}) => {

    console.log(notifications);
    
    return (
        <View>
            <Text>Alert</Text>
        </View>
    );
}

export default AlertScreen;